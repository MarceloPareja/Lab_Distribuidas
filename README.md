## Tarea
Realizar un chat online que tenga las capacidades de:
* Mensajes que se autodestruyen tras un tiempo
* Encriptación de mensajes.
Servidor: Python con Flask
```bash
    python -m venv myvenv
    ./myvenv/Scripts/activate
    pip install Flask Flask-SocketIO eventlet Flask-Cors
    python -m server
```